interface ThreeSize {
  /**
   * Small
   */
  sm: [number, number, number];
  /**
   * Medium
   */
  md: [number, number, number];
  /**
   * Large
   */
  lg: [number, number, number];
}

export default ThreeSize;
