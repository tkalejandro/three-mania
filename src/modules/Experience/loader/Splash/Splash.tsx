import React from 'react';
/**
 * Splash is a concept from Mobile apps
 * Its the first thing that the user sees
 * @returns
 */
const Splash = () => {
  return <div>Splash</div>;
};

export default Splash;
